version: "2"

sql:
- engine: "postgresql"
  queries: "./resources/sql/queries"
  schema: "./resources/sql/schema"
  gen:
    go:
      package: "db"
      out: "./internal/db"
      sql_package: "pgx/v5"
      emit_json_tags: true